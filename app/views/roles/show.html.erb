<div class="container">
  <div class="mt-4">
    <h1><%= @user.first_name %> <%= @user.last_name %></h1>
    <div class="user-container my-4" >
      <div class="user-image">
        <%= image_tag @role.user.photo %>
      </div>
      <div class="user-bio mx-3">
        <small>About Me</small>
        <p><%= @user.bio %></p>
      </div>
    </div>
  </div>
</div>
<%
=begin %>
This can only be implemented once Cloudinary is attached to the user model
<% if @user.photo.attached? %>
  <%= cl_image_path @user.photo.key, crop: :fill %>')">
<% else %>
  <%= image_tag("placeholder-profile.png") %>
<% end %>
<%
=end %>
