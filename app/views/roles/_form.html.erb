<%= simple_form_for @role do |f| %>
  <div class="form-inputs">
    <%= f.input :name, label: 'Role Name', collection: Role::ROLES.sort, include_blank: false %>
    <%= f.input :price, label: 'Price per hour (¥)' %>
    <%= f.input :bio %>
  </div>
  <div class="form-actions">
    <%= f.submit 'Create Service', class: 'btn btn-primary' %>
  </div>
<% end %>
